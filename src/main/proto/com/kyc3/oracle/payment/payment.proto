syntax = 'proto3';

package com.kyc3.oracle.payment;

message Payment {
  // types: ["address", "uint256", "uint256", "uint256", "address"],
  // values: [apAddress, price, paymentNonce, userNonce, cashierAddress],
  string message = 1;
  string signature = 2;
}